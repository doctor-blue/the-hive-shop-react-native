'use strict';

const buttonHeight = 40;

const headerHeight = 60;

const smallText = 16;
const normalText = 24;
const bigText = 32;

const logoSize = 64;
const actionIconSize = 25;
const leadingIconSize = 30;
const iconSpace = 10;

const borderWidth = 1;
const borderRadius = 10;

const tinySpace = 16;
const smallSpace = 24;
const normalSpace = 32;
const bigSpace = 64;

module.exports = {
    buttonHeight,
    headerHeight,
    smallText,
    normalText,
    bigText,
    logoSize,
    actionIconSize,
    leadingIconSize,
    iconSpace,
    borderRadius,
    borderWidth,
    tinySpace,
    smallSpace,
    normalSpace,
    bigSpace
}